'use client';\n\nimport { useState } from 'react';\nimport { Card } from '@/components/ui/Card';\nimport { Button } from '@/components/ui/Button';\nimport { LogicCanvas } from '@/components/canvas/LogicCanvas';\nimport { WorkspaceHeader } from '@/components/layout/WorkspaceHeader';\nimport { ProblemStatement } from '@/components/problems/ProblemStatement';\nimport { AntiHintZone } from '@/components/anti-hint/AntiHintZone';\nimport { LogicPlanEditor } from '@/components/canvas/LogicPlanEditor';\nimport { MOCK_PROBLEMS } from '@/lib/mockData';\nimport { useParams } from 'next/navigation';\n\nexport default function ProblemWorkspace() {\n  const { slug } = useParams();\n  const problem = MOCK_PROBLEMS.find(p => p.slug === slug) || MOCK_PROBLEMS[0];\n  \n  const [isAnalyzing, setIsAnalyzing] = useState(false);\n\n  return (\n    <div className=\"flex flex-col h-dvh bg-[var(--bg-default)]\">\n      <WorkspaceHeader title={problem.title} />\n      \n      <main className=\"flex-1 flex overflow-hidden\">\n        {/* Left: Problem & Plan */}\n        <div className=\"w-[400px] flex flex-col border-r border-[var(--border-default)] bg-[var(--bg-sunken)] p-4 gap-4 overflow-y-auto\">\n          <ProblemStatement problem={problem} />\n          <LogicPlanEditor />\n        </div>\n\n        {/* Center: Canvas */}\n        <div className=\"flex-1 flex flex-col relative\">\n          <div className=\"flex-1 overflow-auto\">\n            <LogicCanvas problemId={problem.id} />\n          </div>\n          \n          {/* Bottom Feedback Zone */}\n          <div className=\"h-[240px] border-t border-[var(--border-default)] bg-[var(--bg-elevated)]\">\n            <AntiHintZone />\n          </div>\n        </div>\n      </main>\n    </div>\n  );\n}\n