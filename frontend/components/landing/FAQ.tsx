'use client';\n\nimport { useState } from 'react';\nimport { motion, AnimatePresence } from 'framer-motion';\nimport { ChevronDown, HelpCircle } from 'lucide-react';\n\nconst FAQS = [\n  {\n    q: \"Why not just use LeetCode and check the solution?\",\n    a: \"Because checking the solution is passive. You understand it after seeing it, but you haven't fixed the logical process that prevented you from reaching it yourself. AlgoMortem fixes the process.\"\n  },\n  {\n    q: \"Does AlgoMortem teach me syntax?\",\n    a: \"No. We assume you know how to write code. We focus entirely on the logical model you build before you ever touch the keyboard.\"\n  },\n  {\n    q: \"What is an 'Anti-Hint'?\",\n    a: \"Traditional hints tell you what to do (e.g., 'Use two pointers'). Anti-Hints tell you what you did wrong (e.g., 'In Step 3, you moved the left pointer, but your plan says to move it only when sum < target. Why did you move it when sum was 10?')\"\n  },\n  {\n    q: \"Is it only for beginners?\",\n    a: \"Hardly. Our most difficult problems (Hard/Expert) require intense mental tracing that even senior engineers often mess up. It's a gym for mental precision.\"\n  }\n];\n\nexport function FAQ() {\n  const [openIdx, setOpenIdx] = useState<number | null>(0);\n\n  return (\n    <section className=\"py-24 px-6\">\n      <div className=\"max-w-3xl mx-auto space-y-12\">\n        <div className=\"text-center space-y-4\">\n          <div className=\"flex items-center justify-center gap-2 text-[var(--brand-primary)]\">\n             <HelpCircle className=\"w-5 h-5\" />\n             <span className=\"font-mono text-[0.65rem] uppercase tracking-widest\">FAQ</span>\n          </div>\n          <h2 className=\"text-3xl font-display font-medium text-[var(--text-primary)]\">\n            Common Post-Mortems.\n          </h2>\n        </div>\n\n        <div className=\"space-y-3\">\n          {FAQS.map((faq, i) => (\n            <div\n              key={i}\n              className=\"rounded-lg border border-[var(--border-default)] bg-[var(--bg-elevated)] overflow-hidden\"\n            >\n              <button\n                onClick={() => setOpenIdx(openIdx === i ? null : i)}\n                className=\"w-full flex items-center justify-between p-5 text-left hover:bg-[var(--bg-sunken)] transition-colors cursor-pointer\"\n              >\n                <span className=\"font-mono text-sm font-semibold uppercase tracking-wide text-[var(--text-primary)]\">\n                  {faq.q}\n                </span>\n                <ChevronDown className={cn('w-4 h-4 text-[var(--text-muted)] transition-transform duration-300', openIdx === i && 'rotate-180')} />\n              </button>\n              <AnimatePresence>\n                {openIdx === i && (\n                  <motion.div\n                    initial={{ height: 0, opacity: 0 }}\n                    animate={{ height: 'auto', opacity: 1 }}\n                    exit={{ height: 0, opacity: 0 }}\n                    transition={{ duration: 0.3, ease: 'easeInOut' }}\n                  >\n                    <div className=\"px-5 pb-5 text-sm text-[var(--text-secondary)] leading-relaxed border-t border-[var(--border-subtle)] pt-4\">\n                      {faq.a}\n                    </div>\n                  </motion.div>\n                )}\n              </AnimatePresence>\n            </div>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n}\n\nfunction cn(...classes: any[]) {\n  return classes.filter(Boolean).join(' ');\n}\n