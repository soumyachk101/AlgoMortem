'use client';\n\nimport { cn } from '@/lib/utils';\nimport type { AttemptHistoryItem } from '@/types/dryRun';\nimport { DifficultyBadge } from '../problems/DifficultyBadge';\n\nexport function AttemptRow({ attempt }: { attempt: AttemptHistoryItem }) {\n  return (\n    <div className=\"flex items-center justify-between p-3 border border-[var(--border-subtle)] rounded hover:border-[var(--border-default)] transition-colors group\">\n      <div className=\"flex items-center gap-4\">\n        <div className=\"flex flex-col\">\n          <span className=\"text-[0.6rem] font-mono text-[var(--text-muted)] uppercase tracking-widest mb-0.5\">\n            {new Date(attempt.createdAt).toLocaleDateString()}\n          </span>\n          <h4 className=\"font-mono text-xs font-bold text-[var(--text-primary)] uppercase\">\n            {attempt.problemTitle}\n          </h4>\n        </div>\n        <DifficultyBadge difficulty={attempt.difficulty} className=\"scale-75 origin-left\" />\n      </div>\n\n      <div className=\"flex items-center gap-6\">\n        <div className=\"flex flex-col items-end\">\n           <span className=\"text-[0.6rem] font-mono text-[var(--text-muted)] uppercase tracking-widest mb-0.5\">Anti-Hints</span>\n           <span className=\"font-mono text-xs text-[var(--text-primary)] tabular-nums\">{attempt.antiHintsUsed}</span>\n        </div>\n        <div className=\"flex flex-col items-end\">\n           <span className=\"text-[0.6rem] font-mono text-[var(--text-muted)] uppercase tracking-widest mb-0.5\">Result</span>\n           <span className={cn(\"font-mono text-xs uppercase tracking-wide\", attempt.isComplete ? \"text-emerald-500\" : \"text-rose-500\")}>\n             {attempt.isComplete ? 'Breakthrough' : 'Failed'}\n           </span>\n        </div>\n      </div>\n    </div>\n  );\n}\n