'use client';\n\nimport { motion, AnimatePresence } from 'framer-motion';\nimport { useDryRunStore } from '@/stores/dryRunStore';\nimport { HelpCircle, Sparkles, AlertTriangle } from 'lucide-react';\n\nexport function AntiHintZone() {\n  const { steps, isAnalyzing } = useDryRunStore();\n\n  return (\n    <div className=\"h-full flex flex-col\">\n      {/* Header */}\n      <div className=\"px-4 h-10 border-b border-[var(--border-default)] flex items-center justify-between\">\n        <div className=\"flex items-center gap-2 text-[var(--brand-primary)]\">\n           <HelpCircle className=\"w-4 h-4\" />\n           <span className=\"font-mono text-[0.65rem] font-bold uppercase tracking-widest\">\n             AI Anti-Hint Console\n           </span>\n        </div>\n        <div className=\"flex items-center gap-2\">\n           <div className=\"w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse\" />\n           <span className=\"text-[0.6rem] font-mono text-[var(--text-muted)] uppercase\">Watching Live...</span>\n        </div>\n      </div>\n\n      {/* Content */}\n      <div className=\"flex-1 p-4 font-mono overflow-y-auto\">\n        <div className=\"max-w-3xl space-y-6\">\n          <AnimatePresence mode='popLayout'>\n            {/* Example Hint */}\n            <motion.div\n              initial={{ opacity: 0, x: -10 }}\n              animate={{ opacity: 1, x: 0 }}\n              className=\"space-y-2 group\"\n            >\n               <div className=\"flex items-center gap-2\">\n                  <span className=\"text-[0.6rem] text-[var(--text-muted)] uppercase\">Lab_Signal_01 // Step 04 triggered</span>\n                  <div className=\"h-px flex-1 bg-[var(--border-subtle)]\" />\n               </div>\n               <p className=\"text-xs text-[var(--text-primary)] leading-relaxed\">\n                 \"At <span className=\"text-[var(--brand-primary)]\">Step 04</span>, your `max_sum` is 10, but in <span className=\"text-[var(--brand-primary)]\">Step 05</span> it remains 10 even though the current element is 15. Does your Logic Plan justify this decision?\"\n               </p>\n               <div className=\"flex items-center gap-3 opacity-0 group-hover:opacity-100 transition-opacity\">\n                 <button className=\"text-[0.6rem] text-[var(--text-muted)] hover:text-[var(--brand-primary)] uppercase\">This was helpful</button>\n                 <button className=\"text-[0.6rem] text-[var(--text-muted)] hover:text-rose-500 uppercase\">Useless</button>\n               </div>\n            </motion.div>\n\n            {/* Another one */}\n            <motion.div\n              initial={{ opacity: 0, x: -10 }}\n              animate={{ opacity: 1, x: 0 }}\n              transition={{ delay: 0.1 }}\n              className=\"space-y-2\"\n            >\n               <div className=\"flex items-center gap-2\">\n                  <span className=\"text-[0.6rem] text-[var(--text-muted)] uppercase\">Lab_Signal_00 // Session Initialized</span>\n                  <div className=\"h-px flex-1 bg-[var(--border-subtle)]\" />\n               </div>\n               <p className=\"text-xs text-[var(--text-muted)]\">\n                 Thinking Dissector initialized. Describe your plan to start the audit.\n               </p>\n            </motion.div>\n          </AnimatePresence>\n        </div>\n      </div>\n    </div>\n  );\n}\n