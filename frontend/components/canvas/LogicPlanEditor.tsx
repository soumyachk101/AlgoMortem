'use client';\n\nimport { useState } from 'react';\nimport { Edit3, CheckCircle2 } from 'lucide-react';\n\nexport function LogicPlanEditor() {\n  const [plan, setPlan] = useState('');\n  const [isSaved, setIsSaved] = useState(false);\n\n  return (\n    <div className=\"flex flex-col gap-3\">\n      <div className=\"flex items-center justify-between\">\n        <div className=\"flex items-center gap-2\">\n          <Edit3 className=\"w-3.5 h-3.5 text-[var(--brand-primary)]\" />\n          <span className=\"font-mono text-[0.65rem] font-bold uppercase tracking-widest text-[var(--text-secondary)]\">\n            Your Logic Plan\n          </span>\n        </div>\n        {isSaved && (\n          <span className=\"flex items-center gap-1 text-[0.6rem] font-mono text-emerald-500 uppercase tracking-widest\">\n            <CheckCircle2 className=\"w-3 h-3\" />\n            Plan Synced\n          </span>\n        )}\n      </div>\n\n      <textarea\n        value={plan}\n        onChange={(e) => { setPlan(e.target.value); setIsSaved(false); }}\n        onBlur={() => plan.length > 10 && setIsSaved(true)}\n        placeholder=\"Describe your algorithm step-by-step. E.g., 'I will use two pointers at both ends and move them based on the current sum...'\"\n        className=\"w-full min-h-[200px] bg-[var(--bg-elevated)] border border-[var(--border-default)] rounded-md p-3 font-mono text-xs text-[var(--text-primary)] leading-relaxed focus:outline-none focus:border-[var(--brand-primary)] placeholder:text-[var(--text-muted)] placeholder:italic\"\n      />\n\n      <p className=\"text-[0.6rem] font-mono text-[var(--text-muted)] leading-tight\">\n        * Be precise. The AI will cross-reference this plan against your actual dry-run steps below.\n      </p>\n    </div>\n  );\n}\n