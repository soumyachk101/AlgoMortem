'use client';\n\nimport { DryRunRow } from './DryRunRow';\nimport { VariableHeaderManager } from './VariableHeaderManager';\nimport { useDryRunStore } from '@/stores/dryRunStore';\nimport { Plus, Info } from 'lucide-react';\nimport { Button } from '@/components/ui/Button';\nimport { AnalyzeButton } from './AnalyzeButton';\n\nexport function LogicCanvas({ problemId }: { problemId: string }) {\n  const { steps, variableHeaders, addStep } = useDryRunStore();\n\n  return (\n    <div className=\"flex flex-col h-full bg-[var(--bg-default)]\">\n      <VariableHeaderManager />\n      \n      {/* Grid Header */}\n      <div className=\"flex border-b border-[var(--border-default)] bg-[var(--bg-sunken)]\">\n        <div className=\"w-12 h-8 flex items-center justify-center font-mono text-[0.6rem] uppercase tracking-widest text-[var(--text-muted)] border-r border-[var(--border-subtle)]\">\n          Step\n        </div>\n        {variableHeaders.map((header) => (\n          <div \n            key={header}\n            className=\"w-24 h-8 flex items-center justify-center font-mono text-[0.65rem] font-bold uppercase tracking-wide text-[var(--text-primary)] border-r border-[var(--border-subtle)]\"\n          >\n            {header}\n          </div>\n        ))}\n        <div className=\"flex-1 h-8 flex items-center px-3 font-mono text-[0.6rem] uppercase tracking-widest text-[var(--text-muted)]\">\n          Logical Deduction\n        </div>\n      </div>\n\n      {/* Rows */}\n      <div className=\"flex-1 overflow-y-auto\">\n        {steps.map((_, i) => (\n          <DryRunRow key={i} stepIndex={i} />\n        ))}\n        \n        {/* Add Row Button */}\n        <div className=\"p-4 flex items-center justify-between\">\n           <button\n            onClick={addStep}\n            className=\"group flex items-center gap-2 font-mono text-[0.65rem] uppercase tracking-widest text-[var(--text-muted)] hover:text-[var(--brand-primary)] transition-colors\"\n          >\n            <span className=\"flex items-center justify-center w-5 h-5 rounded border border-dashed border-[var(--border-default)] group-hover:border-[var(--brand-primary)]\">\n              <Plus className=\"w-3 h-3\" />\n            </span>\n            Add Step\n          </button>\n\n          <AnalyzeButton />\n        </div>\n      </div>\n\n      {/* Tips / Status Bar */}\n      <div className=\"h-8 border-t border-[var(--border-default)] bg-[var(--bg-sunken)] px-4 flex items-center gap-2\">\n        <Info className=\"w-3.5 h-3.5 text-[var(--text-muted)]\" />\n        <span className=\"font-mono text-[0.6rem] text-[var(--text-muted)] uppercase tracking-widest\">\n          Tip: Press 'Tab' to move between variables. 'Shift + Enter' to commit step.\n        </span>\n      </div>\n    </div>\n  );\n}\n