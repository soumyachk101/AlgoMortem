'use client';\n\nimport { Button } from '@/components/ui/Button';\nimport { Check } from 'lucide-react';\n\nexport function StepCommitButton({ onClick, locked }: { onClick: () => void; locked: boolean }) {\n  return (\n    <Button\n      size=\"sm\"\n      onClick={onClick}\n      disabled={locked}\n      variant={locked ? 'ghost' : 'secondary'}\n      className=\"h-7 px-2 font-mono text-[0.6rem] uppercase tracking-widest gap-1.5\"\n    >\n      {locked ? (\n        <span className=\"flex items-center gap-1 opacity-50\">\n          <Check className=\"w-3 h-3\" />\n          Step Locked\n        </span>\n      ) : (\n        'Commit Step'\n      )}\n    </Button>\n  );\n}\n