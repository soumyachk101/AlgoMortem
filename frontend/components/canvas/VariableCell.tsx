'use client';\n\nimport { cn } from '@/lib/utils';\n\ninterface VariableCellProps {\n  value: string;\n  onChange: (val: string) => void;\n  locked?: boolean;\n}\n\nexport function VariableCell({ value, onChange, locked }: VariableCellProps) {\n  return (\n    <div className=\"w-24 h-10 border-r border-b border-[var(--border-subtle)] relative group-cell\">\n      <input\n        disabled={locked}\n        value={value}\n        onChange={(e) => onChange(e.target.value)}\n        className={cn(\n          \"w-full h-full bg-transparent border-none text-center font-mono text-xs focus:outline-none transition-colors\",\n          locked ? \"text-[var(--text-muted)]\" : \"text-[var(--text-primary)] focus:bg-[rgba(255,255,255,0.02)]\",\n          value === '' && \"bg-[rgba(192,57,43,0.02)]\"\n        )}\n        placeholder=\"\u2014\"\n      />\n      {/* Grid interaction glow */}\n      <div className=\"absolute inset-0 border border-transparent pointer-events-none group-focus-within:border-[var(--brand-primary)]/30\" />\n    </div>\n  );\n}\n