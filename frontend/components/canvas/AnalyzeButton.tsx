'use client';\n\nimport { useState } from 'react';\nimport { Button } from '@/components/ui/Button';\nimport { BrainCircuit, Loader2 } from 'lucide-react';\n\nexport function AnalyzeButton() {\n  const [isAnalyzing, setIsAnalyzing] = useState(false);\n\n  const handleAnalyze = () => {\n    setIsAnalyzing(true);\n    setTimeout(() => setIsAnalyzing(false), 2000);\n  };\n\n  return (\n    <Button \n      size=\"sm\" \n      onClick={handleAnalyze}\n      disabled={isAnalyzing}\n      className=\"bg-[var(--brand-glow)] border border-[var(--brand-primary)] text-[var(--brand-primary)] hover:bg-[var(--brand-primary)] hover:text-white\"\n    >\n      {isAnalyzing ? (\n        <Loader2 className=\"w-3.5 h-3.5 animate-spin mr-2\" />\n      ) : (\n        <BrainCircuit className=\"w-3.5 h-3.5 mr-2\" />\n      )}\n      <span className=\"text-[0.65rem] uppercase tracking-widest font-bold\">\n        {isAnalyzing ? 'Analyzing Logic...' : 'Run Logic Audit'}\n      </span>\n    </Button>\n  );\n}\n