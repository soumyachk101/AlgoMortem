import { ProblemCard } from './ProblemCard';\nimport type { ProblemListItem } from '@/types/problem';\n\ninterface ProblemGridProps {\n  problems: ProblemListItem[];\n}\n\nexport function ProblemGrid({ problems }: ProblemGridProps) {\n  if (problems.length === 0) {\n    return (\n      <div className=\"flex flex-col items-center justify-center py-24 text-center space-y-4\">\n        <div className=\"w-16 h-16 rounded-full bg-[var(--bg-sunken)] border border-[var(--border-subtle)] flex items-center justify-center\">\n          <span className=\"font-mono text-2xl text-[var(--text-muted)]\">?</span>\n        </div>\n        <div className=\"space-y-1\">\n          <p className=\"font-mono text-sm text-[var(--text-primary)] uppercase tracking-wide\">No problems found</p>\n          <p className=\"text-xs text-[var(--text-muted)]\">Try adjusting your filters or search query.</p>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6\">\n      {problems.map((problem) => (\n        <ProblemCard key={problem.id} problem={problem} />\n      ))}\n    </div>\n  );\n}\n