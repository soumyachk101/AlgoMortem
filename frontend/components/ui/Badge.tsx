'use client';\n\nimport { Check, Copy } from 'lucide-react';\nimport { useState } from 'react';\nimport { cn } from '@/lib/utils';\n\ninterface BadgeProps {\n  children: React.ReactNode;\n  variant?: 'outline' | 'secondary' | 'brand';\n  className?: string;\n}\n\nexport function Badge({ children, variant = 'outline', className }: BadgeProps) {\n  const variants = {\n    outline: 'border border-[var(--border-default)] text-[var(--text-secondary)]',\n    secondary: 'bg-[var(--bg-elevated)] text-[var(--text-primary)]',\n    brand: 'bg-[var(--brand-glow)] border border-[var(--brand-primary)] text-[var(--brand-primary)]',\n  };\n\n  return (\n    <span className={cn('px-2 py-0.5 rounded text-[0.65rem] font-mono uppercase tracking-wider', variants[variant], className)}>\n      {children}\n    </span>\n  );\n}\n