import { cn } from '@/lib/utils';\n\nexport function Spinner({ className }: { className?: string }) {\n  return (\n    <div className={cn('relative w-6 h-6', className)}>\n      <div className=\"absolute inset-0 border-2 border-[var(--brand-primary)] opacity-20 rounded-full\" />\n      <div className=\"absolute inset-0 border-2 border-t-[var(--brand-primary)] rounded-full animate-spin\" />\n    </div>\n  );\n}\n