import { cn } from '@/lib/utils';\n\ninterface CardProps {\n  children: React.ReactNode;\n  className?: string;\n  hoverable?: boolean;\n}\n\nexport function Card({ children, className, hoverable }: CardProps) {\n  return (\n    <div\n      className={cn(\n        'bg-[var(--bg-elevated)] border border-[var(--border-default)] rounded-lg transition-all duration-300 shadow-sm',\n        hoverable && 'hover:border-[var(--border-strong)] hover:shadow-md hover:translate-y-[-2px]',\n        className\n      )}\n    >\n      {children}\n    </div>\n  );\n}\n