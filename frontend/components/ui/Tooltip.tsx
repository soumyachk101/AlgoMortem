'use client';\n\nimport { useState } from 'react';\nimport { cn } from '@/lib/utils';\n\ninterface TooltipProps {\n  children: React.ReactNode;\n  content: string;\n  className?: string;\n}\n\nexport function Tooltip({ children, content, className }: TooltipProps) {\n  const [show, setShow] = useState(false);\n\n  return (\n    <div className=\"relative inline-block\" onMouseEnter={() => setShow(true)} onMouseLeave={() => setShow(false)}>\n      {children}\n      {show && (\n        <div className={cn('absolute z-50 bottom-full left-1/2 -translate-x-1/2 mb-2 px-2 py-1 bg-[var(--bg-strong)] text-white text-[0.65rem] font-mono rounded pointer-events-none whitespace-nowrap shadow-lg animate-in fade-in zoom-in duration-150', className)}>\n          {content}\n          <div className=\"absolute top-full left-1/2 -translate-x-1/2 border-4 border-transparent border-t-[var(--bg-strong)]\" />\n        </div>\n      )}\n    </div>\n  );\n}\n